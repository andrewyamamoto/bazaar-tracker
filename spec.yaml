name: bazaar-tracker
region: sfo
services:
  - name: web
    environment_slug: python
    source_dir: .
    run_command: "python main.py"
    http_port: 8080
    instance_size_slug: basic-xxs
    instance_count: 1
    routes:
      - path: /

